# ASR service — conda env: student_asr
# Install torch/torchaudio FIRST from cu121 index, then the rest:
#   pip install torch==2.3.1 torchaudio==2.3.1 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/ --trusted-host=mirrors.aliyun.com
#
# Verified working versions on this host (2026-08-09):

torch==2.3.1
torchaudio==2.3.1
funasr==1.4.1
modelscope==1.39.1
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32
numpy==1.26.4
soundfile==0.14.0
aiofiles==25.1.0
pydantic==2.13.4
pydantic-settings==2.15.0
